'use strict';

/**
 * pressure-seal-envelope service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::pressure-seal-envelope.pressure-seal-envelope');
