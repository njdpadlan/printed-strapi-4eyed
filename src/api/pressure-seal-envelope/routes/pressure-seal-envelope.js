'use strict';

/**
 * pressure-seal-envelope router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::pressure-seal-envelope.pressure-seal-envelope');
